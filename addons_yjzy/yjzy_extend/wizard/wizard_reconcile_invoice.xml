<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="wizard_reconcile_invoice_form" model="ir.ui.view">
            <field name="name">wizard.reconcile.invoice.from</field>
            <field name="model">wizard.reconcile.invoice</field>
            <field name="arch" type="xml">
                <form string="创建发运单">
                    <group>
                        <field name="partner_id" groups="base.group_system"/>
                        <field name="yjzy_advance_payment_id" invisible="1"/>
                        <field name="order_id" invisible="1"/>
                        <field name="btd_id" attrs="{'invisible':[('yjzy_type','!=','back_tax')]}" />
                        <field name="yjzy_type" invisible="1"/>
                    </group>

                    <group>
                        <field name="invoice_ids" nolabel="1" domain="[('partner_id','=',partner_id),('state','=','open'),('type','in',['in_invoice'])]"
                               context="{'tree_view_ref':'yjzy_extend.invoice_new_hxd_tree'}"
                               options="{'no_create': True,'no_open':True}"/>
                    </group>
                    <footer>
                        <button name="apply" class="oe_highlight" type='object' string="确认" />
                             <button name="create_yfhxd_new" class="oe_highlight" type='object' string="创建认领" />

                        <button special="cancel" class="oe_highlight" string="取消"/>
                    </footer>
                </form>
            </field>
        </record>

        <!--
              <act_window id="act_wizard_transport4so"
                          name="创建发运单"
                          res_model="wizard.transport4so"
                          view_mode="form"
                          src_model="sale.order"
                          target="new"
                          key2="client_action_multi"
              />
              -->


    </data>
</odoo>

